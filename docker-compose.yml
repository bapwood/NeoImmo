services:
  database:
    container_name: postgres
    image: postgres:alpine
    restart: always
    environment:
      - POSTGRES_USER=${DB_USERNAME}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "${DB_PORT}:${DB_PORT}"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d neoimmo"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - NeoImmo-docker

  server:
    container_name: backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=${SERVER_PORT}
    ports:
      - "${SERVER_PORT}:${SERVER_PORT}"
    depends_on:
      database:
        condition: service_healthy
    volumes:
      - ./backend:/usr/src/app
      - /usr/src/app/node_modules
    networks:
      - NeoImmo-docker
    # command: sh -c "npm run migrate && npm run start:dev"
    command: npm run start:dev

  # front:
  #   container_name: frontend
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   restart: always
  #   volumes:
  #     - ./frontend:/app
  #   depends_on:
  #     - server
  #   ports:
  #     - "${FRONTEND_PORT}:${FRONTEND_PORT}"

  # redis:
  #   container_name: redis
  #   image: redis:alpine
  #   ports:
  #     - "6379:6379"
  #   restart: always
  #   networks:
  #     - NeoImmo-docker

volumes:
  pgdata:

networks:
  NeoImmo-docker:
    driver: bridge
